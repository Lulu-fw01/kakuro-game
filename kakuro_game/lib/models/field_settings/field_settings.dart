/// Class which used as route argument.
/// From [MenuScreen] to [GameScreen].
class FieldSettings {
  final int height;
  final int width;
  final int difficulty;

  FieldSettings(this.height, this.width, this.difficulty); 
}